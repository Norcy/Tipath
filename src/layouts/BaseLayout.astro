---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  compact?: boolean;
}

const {
  title = '小径 · Tipath',
  description = '小径通幽，日有所进。小事之间，自有其法。',
  compact = false
} = Astro.props;

const pageTitle = title === '小径 · Tipath' ? title : `${title} · 小径`;
const base = import.meta.env.BASE_URL;
const canonicalURL = new URL(Astro.url.pathname, Astro.site ?? Astro.url.origin);
const socialTitle = pageTitle;
const socialDescription = description || '小径通幽，日有所进。小事之间，自有其法。';
---
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />

    <title>{pageTitle}</title>
    <meta name="description" content={socialDescription} />
    <link rel="canonical" href={canonicalURL} />

    <meta property="og:type" content="article" />
    <meta property="og:site_name" content="小径 · Tipath" />
    <meta property="og:title" content={socialTitle} />
    <meta property="og:description" content={socialDescription} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:locale" content="zh_CN" />
    <meta property="og:image" content={new URL(`${base}og-image.jpg`, Astro.site ?? Astro.url.origin)} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="小径 · Tipath" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={socialTitle} />
    <meta name="twitter:description" content={socialDescription} />
    <meta name="twitter:image" content={new URL(`${base}og-image.jpg`, Astro.site ?? Astro.url.origin)} />

    <meta name="theme-color" content="#0f5a43" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <link rel="manifest" href={`${base}site.webmanifest`} />
  </head>
  <body>
    <div class="site-shell">
      {compact ? (
        <header class="site-header site-header-compact">
          <a class="compact-brand" href={base}>小径</a>
        </header>
      ) : (
        <header class="site-header">
          <a class="brand" href={base}>
            <span class="brand-cn">小径</span>
            <span class="brand-en">Tipath</span>
          </a>
          <p class="slogan">小径通幽，日有所进。小事之间，自有其法。</p>
        </header>
      )}
      <main>
        <slot />
      </main>
      {!compact && (
        <footer class="site-footer">
          <span>Tipath</span>
          <span>小径通幽</span>
        </footer>
      )}
    </div>
  </body>
</html>
